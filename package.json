{
  "name": "fetch-monorepo",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "workspaces": [
    "packages/*",
    "tooling/*"
  ],
  "devDependencies": {
    "@fetch-monorepo/linting": "*",
    "@fetch-monorepo/testing": "*",
    "@fetch-monorepo/typescript": "*"
  },
  "scripts": {
    "build": "tsc --build",
    "lint": "eslint",
    "test": "vitest",
    "clean": "rm -rf packages/*/dist"
  }
}
